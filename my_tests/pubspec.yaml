name: my_tests
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.2
  text_analysis: ^1.0.0
  cupertino_icons: ^1.0.6
  modal_progress_hud_nsn: ^0.5.1
  fl_chart: ^0.69.2  # إضافة مكتبة fl_chart


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/profilePersonaly.png
